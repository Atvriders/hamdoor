# Copy to .env and fill in. docker compose reads this automatically.
# Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
# Optional overrides:
# PORT=3033
# REGISTRY=ghcr.io/atvriders
# TAG=latest
